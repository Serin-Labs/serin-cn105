# Pin the builder and its Python dependencies so an upstream release cannot
# silently change the release environment. This ESPHome version is what
# firmware/esphome/ was built with and what docs/esphome-rest-api.md is
# verified against; bump all three together. Dependabot proposes the bumps.
#
# The pin governs the prebuilt binaries only. Home Assistant users adopting
# via dashboard_import compile with their own ESPHome, for which the contract
# is `min_version` in esphome/common/esp32*.yaml.
-c requirements-build.lock
esphome==2026.7.4
